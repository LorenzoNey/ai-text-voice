<Window x:Class="WisprClone.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WisprClone Settings"
        Icon="/Resources/Icons/app_icon.ico"
        Width="480" Height="620"
        MinWidth="400" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#FF1E1E1E">

    <Window.Resources>
        <!-- Subtle scrollbar thumb style -->
        <Style x:Key="ScrollBarThumb" TargetType="Thumb">
            <Setter Property="Width" Value="3"/>
            <Setter Property="Background" Value="#60FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="1.5"
                                Width="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#90FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Subtle vertical scrollbar style -->
        <Style x:Key="DarkScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Transparent" Width="6">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Width="3">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Subtle ScrollViewer style -->
        <Style x:Key="DarkScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Margin="4,0,0,0"
                                       Style="{StaticResource DarkScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF3391FF"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,16,0,12"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFB0B0B0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF3391FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="10,0,0,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  IsHitTestVisible="False"/>
                                <Path Grid.Column="1"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 5 5 L 10 0 Z"
                                      Fill="#FF808080"/>
                            </Grid>
                            <ToggleButton Grid.ColumnSpan="2"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Opacity="0"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#FF2D2D2D"
                                        BorderBrush="#FF404040"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,2,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200">
                                    <ScrollViewer Style="{StaticResource DarkScrollViewer}"
                                                  VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF505050"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF3391FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFE0E0E0"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#FF3D3D3D"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#FF3391FF"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFB0B0B0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder"
                                    Grid.Column="0"
                                    Width="18" Height="18"
                                    Background="#FF2D2D2D"
                                    BorderBrush="#FF404040"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="M 2 5 L 5 8 L 10 2"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Margin="1"/>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#FF3391FF"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF3391FF"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF3391FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF4DA3FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF2878D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#FF3D3D3D"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF353535"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24,24,6,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Style="{StaticResource DarkScrollViewer}">
            <StackPanel>
                <!-- Speech Provider Selection -->
                <TextBlock Text="Speech Provider"
                           Style="{StaticResource SectionHeaderStyle}"
                           Margin="0,0,0,12"/>

                <TextBlock Text="Select Provider" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="SpeechProviderComboBox" Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Offline (Windows Speech)" Tag="Offline" IsSelected="True"/>
                    <ComboBoxItem Content="Azure Speech Service" Tag="Azure"/>
                    <ComboBoxItem Content="OpenAI Whisper" Tag="OpenAI"/>
                </ComboBox>

                <TextBlock x:Name="ProviderInfoText"
                           Text="Note: Changing the speech provider requires an app restart to take effect."
                           Foreground="#FF808080"
                           FontSize="11"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"/>

                <!-- Azure Settings -->
                <TextBlock Text="Azure Speech Service"
                           Style="{StaticResource SectionHeaderStyle}"
                           Margin="0,16,0,12"/>

                <TextBlock Text="Subscription Key" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="AzureKeyTextBox" Style="{StaticResource TextBoxStyle}"/>

                <TextBlock Text="Region" Style="{StaticResource LabelStyle}" Margin="0,12,0,6"/>
                <TextBox x:Name="AzureRegionTextBox" Style="{StaticResource TextBoxStyle}"/>

                <CheckBox x:Name="UseAzureFallbackCheckBox"
                          Content="Use Azure as fallback when offline recognition fails"
                          Style="{StaticResource CheckBoxStyle}"
                          Margin="0,12,0,0"/>

                <!-- OpenAI Whisper Settings -->
                <TextBlock Text="OpenAI Whisper"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <TextBlock Text="API Key" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="OpenAIKeyTextBox" Style="{StaticResource TextBoxStyle}"/>

                <TextBlock Text="Note: Whisper transcribes after you stop speaking (no real-time results)."
                           Foreground="#FF808080"
                           FontSize="11"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"/>

                <!-- Language Settings -->
                <TextBlock Text="Language"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <TextBlock Text="Recognition Language" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="LanguageComboBox" Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="English (US)" Tag="en-US" IsSelected="True"/>
                    <ComboBoxItem Content="English (UK)" Tag="en-GB"/>
                    <ComboBoxItem Content="Romanian" Tag="ro-RO"/>
                    <ComboBoxItem Content="Spanish" Tag="es-ES"/>
                    <ComboBoxItem Content="French" Tag="fr-FR"/>
                    <ComboBoxItem Content="German" Tag="de-DE"/>
                    <ComboBoxItem Content="Italian" Tag="it-IT"/>
                    <ComboBoxItem Content="Portuguese" Tag="pt-BR"/>
                    <ComboBoxItem Content="Chinese (Simplified)" Tag="zh-CN"/>
                    <ComboBoxItem Content="Japanese" Tag="ja-JP"/>
                </ComboBox>

                <!-- Hotkey Settings -->
                <TextBlock Text="Hotkey"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Double-tap interval (ms)" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="DoubleTapIntervalTextBox" Style="{StaticResource TextBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Max key hold (ms)" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="MaxKeyHoldTextBox" Style="{StaticResource TextBoxStyle}"/>
                    </StackPanel>
                </Grid>

                <!-- Recording Limits -->
                <TextBlock Text="Recording Limits"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <TextBlock Text="Maximum recording duration (seconds)" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="MaxRecordingDurationTextBox" Style="{StaticResource TextBoxStyle}" Width="120" HorizontalAlignment="Left"/>
                <TextBlock Text="Recording will automatically stop after this duration (10-600 seconds)."
                           Foreground="#FF808080"
                           FontSize="11"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"/>

                <!-- Behavior Settings -->
                <TextBlock Text="Behavior"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <CheckBox x:Name="AutoCopyCheckBox"
                          Content="Automatically copy transcription to clipboard"
                          Style="{StaticResource CheckBoxStyle}"
                          Margin="0"/>

                <CheckBox x:Name="StartMinimizedCheckBox"
                          Content="Start minimized to system tray"
                          Style="{StaticResource CheckBoxStyle}"/>

                <CheckBox x:Name="MinimizeToTrayCheckBox"
                          Content="Minimize to system tray instead of taskbar"
                          Style="{StaticResource CheckBoxStyle}"/>

                <!-- Debugging -->
                <TextBlock Text="Debugging"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <CheckBox x:Name="EnableLoggingCheckBox"
                          Content="Enable logging"
                          Style="{StaticResource CheckBoxStyle}"
                          Margin="0"/>
                <TextBlock Text="Logs are saved to %APPDATA%\WisprClone\logs"
                           Foreground="#FF808080"
                           FontSize="11"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"/>

                <!-- About -->
                <TextBlock Text="About"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="WisprClone"
                               Foreground="#FFE0E0E0"
                               FontSize="14"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="VersionText"
                               Text="v1.0.0"
                               Foreground="#FF808080"
                               FontSize="12"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="A voice-to-text transcription tool for Windows."
                           Foreground="#FF808080"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"/>

                <TextBlock Text="Double-tap Ctrl to start/stop dictation."
                           Foreground="#FF808080"
                           FontSize="11"
                           TextWrapping="Wrap"/>

                <Border Height="16"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="CancelButton_Click"
                    Margin="0,0,12,0"/>
            <Button Content="Save"
                    Style="{StaticResource ButtonStyle}"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
